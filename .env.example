# Copy this file to .env and fill in your values.
# For local development, use the Railway EXTERNAL (TCP proxy) connection URL,
# not the internal postgres.railway.internal URL (which only works within Railway's network).
# Find it in: Railway console → PostgreSQL service → Connect → Public Network

DATABASE_URL=postgresql://postgres:<password>@<host>:<port>/railway
