# DBGAPS Dashboard 수정 실행계획 v3

> 오케스트레이터 핸드오프용 문서.
> 코드 작성 전 반드시 루트 `CLAUDE.md`와 `docs/DESIGN.md`를 먼저 읽을 것.
> 작업 완료 기준은 각 태스크 하단 **검증** 항목 충족.

---

## 실행 순서

Phase 1(P0) → Phase 2(P1) 순서로 진행. P1 태스크는 서로 독립적이므로 병렬 실행 가능.
단, **P1-4(갱신 버튼)는 P0-2 제거가 선행**되어야 함.

---

## Phase 1 — P0 (즉시 수정, 버그)

---

### P0-1 · fmtKRW divisor 버그

**파일**: `frontend/app/page.tsx:120`

**현재 코드**:
```ts
function fmtKRW(value: number) {
  if (value >= 1_000_000_000) {
    return `${(value / 1_000_000_000).toFixed(2)}억원`;
  }
  if (value >= 1_000_000) {
    return `${(value / 1_000_000).toFixed(0)}만원`;
  }
  return `${value.toLocaleString()}원`;
}
```

**수정 후**:
```ts
function fmtKRW(value: number) {
  if (value >= 100_000_000) {
    return `${(value / 100_000_000).toFixed(2)}억원`;
  }
  if (value >= 10_000) {
    return `${(value / 10_000).toFixed(0)}만원`;
  }
  return `${value.toLocaleString()}원`;
}
```

**변경 이유**:
- threshold `1_000_000_000` → `100_000_000`: 1억 미만만 만원 표기, 나머지는 억원 표기
- divisor `1_000_000_000` → `100_000_000`: 10억을 10억으로 나누면 1.03이 나와 "1.03억원" 오표시
- 만원 threshold도 `1_000_000` → `10_000`로 교정 (1만원~1억원 구간 커버)

**검증**: 총 자산 현황 카드에서 "10.34억원" 형태로 표시되는지 확인.

---

### P0-2 · api/main.py 임시 엔드포인트 예약 제거

**파일**: `api/main.py`

현재 `main.py:31~42`에 동기식 임시 엔드포인트가 삽입되어 있음. P1-4 완료 후 아래 블록 삭제:

```python
# 삭제 대상
@app.post("/api/refresh-prices")
def refresh_prices() -> dict[str, object]:
    result = subprocess.run(
        ["python", "src/update_prices.py"],
        capture_output=True,
        text=True,
    )
    return { ... }
```

`import subprocess`도 함께 제거.

> **주의**: P1-4 구현 전에는 삭제하지 말 것. P1-4 완료 확인 후 제거.

**검증**: `main.py`에 `subprocess` import 없음. `/api/refresh-prices` 라우트는 `dashboard.py`에만 존재.

---

### P0-3 · 업데이트 로그 갱신

**작업**: 아래 명령 실행 후 결과 파일 커밋·푸시.

```bash
python scripts/update_changelog.py
git add data/CHANGELOG.json
git commit -m "chore: CHANGELOG.json 갱신 (5.26 이후 커밋 반영)"
git push
```

**검증**: `data/CHANGELOG.json` 첫 항목 date가 `2026-05-26` 이상인지 확인.

---

### P0-4 · 매도 선택 시 레이블 버그

**파일**: `frontend/app/trades/page.tsx`

매매 기록 입력 폼에서 단가 입력 필드의 레이블을 조건부 렌더링.

**수정 방향**: 구분(action) state가 `"매도"`이면 `"매도단가(원)"`, 그 외엔 `"매수단가(원)"`.

예시 패턴:
```tsx
<label>{action === "매도" ? "매도단가(원)" : "매수단가(원)"}</label>
```

**검증**: 입력 폼에서 구분을 "매도"로 변경 시 레이블이 즉시 바뀌는지 확인.

---

### P0-5 · 회전율 타임스케일 검증

**파일**: `src/turnover.py`, `output/turnover_initial.csv`, `output/turnover_monthly.csv`

**확인 항목**:
1. 초기 누적 기간 `2026-06-01 ~ 2026-06-08`이 현재 날짜 기준 미도래 → 0% 표시 중. 미도래 기간의 회전율은 `N/A` 또는 `대기중` 등 의미 있는 값으로 표시되는 로직이 있는지 확인.
2. `turnover.py`에서 날짜 범위 계산 시 `today`를 `datetime.date.today()` 또는 `pd.Timestamp.today()`로 동적 참조하는지 확인 (하드코딩된 날짜면 버그).
3. 대회 규칙 기준: 초기 80% 이상, 이후 월 10% 이상.

**처리**: 검증 결과를 사용자에게 보고 후 수정 범위 결정. 단독으로 코드 수정하지 말 것.

---

## Phase 2 — P1 (이번 스프린트)

---

### P1-1 · 히트맵 카드 — 월별 바차트 삭제

**파일**: `frontend/app/page.tsx` (일별 수익률 히트맵 카드 섹션)

**작업**:
1. `MonthlyBarChart` 컴포넌트 렌더링 코드 제거
2. `MonthlyBarChart` import 제거 (orphan 방지)
3. `DailyHeatmap`이 카드 전체 너비를 차지하도록 레이아웃 수정 (grid 비율 또는 flex 변경)

**검증**: 히트맵 카드에 바차트가 없고 히트맵만 카드 전체 폭으로 표시됨.

---

### P1-2 · 총 자산 현황 카드

#### (a) 레이블 수정
**파일**: `frontend/app/page.tsx` 또는 `frontend/components/ui/KpiStrip.tsx`

"평가 금액 (ETF)" → "평가 금액" 문자열 수정.

#### (b) 파이차트 3개로 분리

**파일**: `frontend/app/page.tsx` 총 자산 현황 카드 섹션 (현재 `DonutChart` 1개)

**데이터 출처**: `useLiveHoldings()` 응답 (`LiveHolding[]`). 각 항목에 `risk_type`, `asset_class`, `market_value`, `cost_basis` 포함.

**현금 잔고 계산**:
```ts
const INITIAL_CAPITAL = 1_000_000_000; // 10억 (고정 상수)
const totalCostBasis = holdings.reduce((sum, h) => sum + h.cost_basis, 0);
const cashBalance = INITIAL_CAPITAL - totalCostBasis;
```

**차트 3개 구성**:

| 차트 | 제목 | 데이터 |
|------|------|--------|
| ① | 현금 / 투자자산 | `cashBalance` vs `market_value` 합산 |
| ② | 위험자산 / 안전자산 | `risk_type === "위험"` vs `"안전"` market_value 합산 |
| ③ | 자산군별 | `asset_class` 기준 market_value 합산 |

**구현**: 기존 `PieChart` 컴포넌트(`frontend/components/charts/PieChart.tsx`)를 3회 인스턴스화. 카드 내부를 3등분 레이아웃으로 변경.

**검증**: 3개 파이차트 렌더링. ① 합산이 100%. ② 합산이 100%. ③ 합산이 100%.

---

### P1-3 · 투자 규칙 카드 2열 레이아웃

**파일**: `frontend/app/page.tsx` 투자 규칙 카드 섹션

**현재**: `liveRulesQuery.data.individual[]` + `risk_asset`을 세로 단일 목록으로 나열.

**수정**: `grid-cols-2` 2열 레이아웃으로 변경.
- **왼쪽 열**: `individual` 중 개별 ETF 항목 (code가 `"["` 로 시작하지 않는 항목)
- **오른쪽 열**: `individual` 중 자산군 집계 항목 (code가 `"["` 로 시작) + `risk_asset` 항목

필터 예시:
```ts
const etfRules = rules.individual.filter(r => !r.code.startsWith("["));
const sectorRules = rules.individual.filter(r => r.code.startsWith("["));
```

**검증**: 개별 ETF 항목과 섹터/위험자산 항목이 좌우로 나뉘어 표시됨.

---

### P1-4 · 현재가 수동 갱신 버튼

#### 백엔드

**파일**: `api/routers/dashboard.py`

**추가 내용**:

```python
# 모듈 최상단 (import 영역)
import subprocess
import threading
import time
from fastapi import BackgroundTasks

# 모듈 수준 상태 변수
_refresh_state: dict = {"status": "idle", "updated_at": ""}

def _run_refresh() -> None:
    global _refresh_state
    try:
        subprocess.run(
            ["python", "src/update_prices.py"],
            capture_output=True,
            text=True,
        )
        _refresh_state = {"status": "done", "updated_at": pd.Timestamp.now().isoformat()}
    except Exception:
        _refresh_state = {"status": "error", "updated_at": pd.Timestamp.now().isoformat()}

@router.post("/refresh-prices", status_code=202)
def refresh_prices(background_tasks: BackgroundTasks) -> dict:
    global _refresh_state
    if _refresh_state["status"] == "running":
        return {"status": "already_running"}
    _refresh_state = {"status": "running", "updated_at": ""}
    background_tasks.add_task(_run_refresh)
    return {"status": "started"}

@router.get("/refresh-status")
def refresh_status() -> dict:
    return _refresh_state
```

#### 프론트엔드

**파일**: `frontend/app/page.tsx` 또는 `frontend/components/ui/StatusBar.tsx`

**추가 내용**:
- "현재가 갱신" 버튼 (AS-OF 날짜 옆 배치)
- 클릭 → `POST /api/refresh-prices`
- 갱신 중: 버튼 비활성화 + 로딩 텍스트
- `setInterval` 3초마다 `GET /api/refresh-status` 폴링
- status `"done"` 또는 `"error"` 수신 시: 폴링 종료 + 쿼리 무효화 (`queryClient.invalidateQueries`)

#### P0-2 제거

P1-4 완료 확인 후 `api/main.py`의 임시 동기식 엔드포인트 블록 및 `import subprocess` 제거.

**검증**:
1. 버튼 클릭 → 202 즉시 응답 (타임아웃 없음)
2. 폴링으로 status 변화 추적
3. `main.py`에 임시 코드 없음

---

### P1-5 · 이력 카드 accordion 재구성

**파일**: `frontend/app/trades/page.tsx`

**현재**: "이유" 컬럼이 행에 표시되어 잘림.

**수정**:

1. **기본 노출 컬럼** (테이블 헤더): `날짜`, `구분`, `ETF코드`, `ETF명`, `비중전`, `비중후`, `수량`, `단가`, `거래금액`, `수익/손실`
   - "이유" 컬럼 헤더 및 셀 제거
   - "거래금액" 위치 이동 (수량 × 단가로 프론트 계산: `quantity * price`)

2. **행 클릭 accordion**: 행 클릭 토글 시 해당 행 아래에 확장 영역(`<tr>`) 렌더링 → "이유: {reason}" 텍스트 표시.

상태 예시:
```ts
const [expandedId, setExpandedId] = useState<number | null>(null);
```

**검증**: 이유 텍스트가 기본 숨김. 행 클릭 시 이유 표시, 재클릭 시 닫힘.

---

### P1-6 · 대회 룰 탭 구축 + 실시간 위반 표시

**파일**: `frontend/app/rules/page.tsx` (현재 플레이스홀더)

**데이터**: `useLiveRules()` 훅 사용 (`frontend/lib/hooks/dashboard.ts:286`에 이미 정의됨).

**응답 구조**:
```ts
// RulesResponse
{
  individual: IndividualRule[],  // ETF별 + 자산군별 규칙
  risk_asset: RiskAssetRule      // 위험자산 70% 규칙
}
// IndividualRule: { code, name, current_weight, limit, excess, passed }
// RiskAssetRule:  { rule, risky_weight, limit, excess, passed }
```

**페이지 구성**:

```
[섹션 1] 평가 구조
  - 30점/70점 테이블 (정적 JSX)

[섹션 2] 주요 일정
  - 날짜·제출물 테이블 (정적 JSX)
  - 대회 기간 2026.6.1~8.31, 초기 포트폴리오 ~6.8 등

[섹션 3] 컷오프 및 주의 규칙
  - 경고 배지가 붙은 항목 리스트 (정적 JSX)

[섹션 4] 편입 비중 상한 테이블 ← 실시간 위반 표시 연동
  - 행 구성: 구분 | 자산군 | 비중 상한 | 현재 비중 | 상태
  - 현재 비중 + 통과/위반 배지: useLiveRules() 데이터 매핑

  매핑 키:
    개별 ETF 상한(20%) → individual 항목 중 !code.startsWith("[")
    위험자산 70% → risk_asset
    세부 자산군 → individual 항목 중 code === `[${asset_class}]` 형태
      예: code="[국내주식_섹터]" → 국내주식_섹터 행에 연결

[섹션 5] 운용 시 주의사항
  - 카드 목록 또는 아코디언 (정적 JSX)
  - "수익률보다 설명 가능한 운용", "전망이 틀렸을 때 기준", 등
```

**주의**:
- `DB GAPS 투자대회 핵심 규칙 및 평가요소.md` 내 `![[Pasted image 20260524172843.png]]` 줄 → 제외
- Obsidian `[[링크]]` 문법 → 제외 또는 plain text 처리

**검증**:
- 섹션 4 테이블에서 위반 중인 항목에 빨간 "위반" 배지, 통과 항목에 초록 "통과" 배지 표시
- 데이터 로딩 중엔 로딩 표시, 데이터 없으면 "보유 종목 없음" 안내

---

### P1-7 · NAV / MDD 차트 Y축 조정

**파일**:
- `frontend/components/charts/NavChart.tsx`
- `frontend/components/charts/DrawdownChart.tsx`

**목표**: 데이터 변화폭이 시각적으로 더 잘 보이도록 Y축 여백 추가.

**TradingView Lightweight Charts v5 기준**:
```ts
chart.priceScale("right").applyOptions({
  scaleMargins: { top: 0.1, bottom: 0.1 },
  autoScale: true,
});
```

또는 시리즈에 `priceScaleId` 지정 후 min/max 수동 제어.

**검증**: NAV/MDD 차트에서 데이터 변화가 눈에 띄게 표시됨. Y축이 0~최대값으로 고정되지 않음.

---

### P1-8 · groupMap 연결 (비교 탭 그룹핑 즉시 활성화)

**파일**: `frontend/app/comparison/page.tsx:266`

**현재 코드**:
```ts
const groupMap: Record<string, string | null | undefined> = {};
```

**수정 방향**:
1. `usePortfolios()` 훅 (또는 `useQuery`로 `GET /api/portfolios` 직접 호출)으로 포트폴리오 목록 가져오기
2. `groupMap`을 `{ [portfolio.name]: portfolio.group_name }` 형태로 구성

예시:
```ts
const portfoliosQuery = useQuery({
  queryKey: ["portfolios"],
  queryFn: () => get<{ name: string; group_name: string | null }[]>("/api/portfolios"),
});

const groupMap = useMemo(
  () => Object.fromEntries(
    (portfoliosQuery.data ?? []).map(p => [p.name, p.group_name])
  ),
  [portfoliosQuery.data]
);
```

기존 `groupPortfolios()` 함수는 그대로 사용. 백엔드 수정 없음.

**검증**: 비교 탭에서 DB에 저장된 `group_name` 값대로 포트폴리오가 그룹핑되어 표시됨.

---

## 전체 완료 후 체크리스트

```
[ ] P0-1  fmtKRW: 총 자산 10.3X억원으로 표시
[ ] P0-2  main.py: 임시 엔드포인트 없음
[ ] P0-3  CHANGELOG.json: 2026-05-26 커밋 반영됨
[ ] P0-4  매매일지: 매도 선택 시 레이블 "매도단가(원)"
[ ] P0-5  회전율: 타임스케일 검증 결과 보고됨
[ ] P1-1  히트맵 카드: 바차트 없음, 히트맵 전체 폭
[ ] P1-2a 총 자산 카드: "평가 금액 (ETF)" → "평가 금액"
[ ] P1-2b 총 자산 카드: 파이차트 3개 렌더링, 각 합산 100%
[ ] P1-3  규칙 카드: 좌우 2열 레이아웃
[ ] P1-4  갱신 버튼: 202 즉시 응답, 폴링 동작, main.py 정리
[ ] P1-5  이력 카드: 거래금액 노출, 이유 accordion
[ ] P1-6  대회 룰 탭: 5개 섹션, 실시간 통과/위반 배지
[ ] P1-7  차트: NAV/MDD Y축 여백 적용
[ ] P1-8  비교 탭: group_name 기준 그룹핑 활성화
```

---

## 참고 — 건드리지 않는 것

- `output/` 파일: 엔진 실행 결과물, 수동 편집 금지
- `api/routers/portfolios.py` 외의 `api/` 파일: `src/` import 금지 (CRITICAL-1)
- `useCurrentHoldings()`: 레거시, 신규 UI에서 사용 금지 → `useLiveHoldings()` 사용
- `useRules()`: 레거시 → `useLiveRules()` 사용 (SYNC-6)
- `/operations` 라우트: `redirect("/")` 래퍼, 수정 금지

---

## 다음 스프린트 예약 (P2)

- 비교 탭 그룹 편집 UI: 그룹명 인라인 편집 + 새 그룹 추가 + 드래그앤드롭 배정 (`@dnd-kit/core`)
- `PATCH /api/portfolios/:name` 엔드포인트 추가 필요
